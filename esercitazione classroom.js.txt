 
const nome= prompt("qual è il tuo nome")
const cognome= prompt ("qual è il tuo conome")
const indirizzo= prompt("qual è il tuo indirizzo")
const città= prompt ("dove vivi")
const cap= prompt ("inserisci il CAP")
const annoNascita = prompt ("quando sei nato")
generazioneAnno{
    if (anno>=1997) return "fai parte della generazione Z"
    else if (anno>=1981) return "fai parte della generazione Millenials"
    else if (anno>=1965) return "fai parte della generazione X"
    else if (anno>=1946) return "fai parte delle generazione Baby boomer"
    else if (anno>=1928) return "fai parte della generazione Silenziosa"
    else if (anno>=1901) return "fai parte della generazione Greatwst Generation"
}